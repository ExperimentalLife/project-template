import { DashboardComponent } from './dashboard.component';
import { Routes, RouterModule } from '@angular/router';

export const routes: Routes = [
  
];


export const routingDashboard = RouterModule.forChild(routes);